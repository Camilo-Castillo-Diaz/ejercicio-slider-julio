type person ={
    name : string
    code : string| number
    description?: string
}
const newPersona : person ={
    code :"h",
    name : "camilo"
}

type service1 = string | null| number|undefined
type service = "admin" | "normal" | "superuse"

let response : service1
const camilo :service = "superuse"

